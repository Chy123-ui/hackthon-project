# AGENT.md

## 项目原则

本项目目标：

> 最短时间完成可演示版本（Demo First）。

Agent必须优先完成真实功能。

禁止为了“架构完整性”牺牲开发速度。

---

# 第一原则

不要猜测需求。

不要自作主张增加功能。

严格按照当前任务开发。

未被要求的内容一律不做。

---

# 禁止事项

禁止出现：

* Mock数据
* Fake数据
* Demo数据
* 测试剧情
* 示例回复
* 占位内容
* TODO功能
* 临时实现
* 后续再接入
* 模拟接口
* 模拟AI回复

例如：

错误：

```js
const mockReply = "你来到了一片森林";
```

错误：

```js
setTimeout(() => {
  resolve("AI回复");
}, 1000);
```

错误：

```js
return {
  message: "测试回复"
}
```

如果需要AI回复：

必须直接调用真实API。

---

# API原则

禁止伪造后端。

禁止伪造接口。

禁止伪造返回值。

要求：

```text
前端
 ↓
Express
 ↓
DeepSeek
 ↓
返回结果
```

必须走真实链路。

---

# 开发原则

每次只完成当前任务。

不要提前开发下一阶段功能。

不要进行大规模重构。

不要修改无关文件。

不要优化未被要求的模块。

---

# 输出要求

完成任务后必须停止。

输出：

## 修改文件

* xxx
* xxx

## 新增文件

* xxx

## 实现内容

...

## 测试方法

...

然后等待验收。

禁止继续开发下一项。

---

# 代码原则

优先：

简单

直接

可运行

可演示

不要过度设计。

禁止出现：

* Repository层
* Factory模式
* Builder模式
* DDD
* CQRS
* 复杂抽象

除非明确要求。

---

# UI原则

优先实现功能。

不要花时间：

* 美化动画
* 设计系统
* 主题系统
* 国际化
* 响应式优化

除非明确要求。

---

# AI项目特殊要求

本项目为AI文字冒险游戏。

所有剧情内容必须来自真实AI。

禁止硬编码剧情。

禁止预设剧情。

禁止固定回复模板。

禁止本地剧情生成器。

所有剧情来源：

DeepSeek API。

---

# 遇到问题时

优先：

修复问题

不要绕过问题。

不要降级为Mock方案。

不要改成假数据方案。

不要写：

"暂时使用模拟数据代替"

必须解决真实问题。

---

# 最终原则

如果有两种方案：

方案A：
真实可运行

方案B：
模拟实现

永远选择方案A。
